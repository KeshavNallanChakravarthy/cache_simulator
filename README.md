# cache_simulator
A fully configurable C++ cache simulator that models direct-mapped, set-associative, and fully associative caches with multiple replacement and write policies. It generates detailed performance statistics, supports various synthetic memory-access patterns, and includes CSV export and comparison tools for analyzing cache behavior.
